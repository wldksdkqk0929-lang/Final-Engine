name: Final Engine

on:
  workflow_dispatch:
  schedule:
    - cron: "5 15 * * *"

# [핵심] 일꾼에게 "전시장 건설(쓰기)" 권한 부여
permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests yfinance pyyaml deep-translator

      - name: Run Sniper Engine
        run: python run.py

      # 👇 [이게 없어서 안 생긴 겁니다!] 전시장(gh-pages) 건설 및 배포 단계
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./data/artifacts/dashboard
          force_orphan: true  # 기존 내용 덮어쓰기
