[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sniper-v12"
version = "0.1.0"
description = "SNIPER V12 Algorithmic Trading Engine"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["engine*"]
